#include<iostream>
using namespace std;
int main ()
{
    int a,b;
    cout<<"Enter the value of a and b : "<<endl;
    cin>>a>>b;
    cout<<"(a*a*a-b*b*b) = "<<(a*a*a-b*b*b);
    return 0;
}